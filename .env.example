# =============================================================================
# Intel Desk - Next.js App Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase
# -----------------------------------------------------------------------------
# Create a free project at https://supabase.com/dashboard
# Find these values in: Project Settings > API
# The publishable key is safe to expose in the browser (row-level security enforced).
# The secret key must NEVER be exposed client-side.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-key
SUPABASE_SECRET_KEY=sb_secret_your-key

# Database password you chose when creating the Supabase project.
# Found in: Project Settings > Database > Connection string
SUPABASE_PASSWORD=your-supabase-db-password

# -----------------------------------------------------------------------------
# AI Providers
# -----------------------------------------------------------------------------
# OpenAI - Used for GPT-based analysis and summarization.
# Get your API key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-key

# Perplexity - Used for real-time web-augmented intelligence queries.
# Get your API key at https://docs.perplexity.ai/ (Settings > API)
PERPLEXITY_API_KEY=pplx-your-perplexity-key

# -----------------------------------------------------------------------------
# Market Data (optional)
# -----------------------------------------------------------------------------
# These providers enrich the economic/financial intelligence modules.
# The app works without them, but market-related features will be limited.

# Finnhub - Real-time stock and forex data.
# Sign up for a free key at https://finnhub.io/register
FINNHUB_API_KEY=your-finnhub-key

# Alpha Vantage - Historical market data and technical indicators.
# Claim a free key at https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key

# -----------------------------------------------------------------------------
# Cron Security
# -----------------------------------------------------------------------------
# A secret token used to authenticate scheduled cron job requests
# (e.g., Vercel Cron or any external scheduler hitting /api/cron/*).
# Generate a strong random string, for example:
#   openssl rand -base64 32
CRON_SECRET=generate-a-secure-random-string

# -----------------------------------------------------------------------------
# Mapbox (optional - for 3D globe map)
# -----------------------------------------------------------------------------
# Get a free public token at https://account.mapbox.com/
# Used only for the interactive globe map - app works without it.
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your-mapbox-public-token

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
# The public-facing URL of your app. Used for building absolute links,
# OAuth redirects, and Open Graph metadata.
# In production, set this to your deployed domain (e.g., https://inteldesk.example.com).
NEXT_PUBLIC_APP_URL=http://localhost:3000
